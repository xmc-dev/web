import { Component, h } from 'preact';
import { Header, Container } from 'semantic-ui-react';

export default class Editor extends Component {
	render() {
		return (
			<Container>
				<Header>Page editor</Header>
			</Container>
		);
	}
}
