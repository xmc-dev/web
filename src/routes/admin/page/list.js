import { h } from 'preact';
import { Container, Header } from 'semantic-ui-react';

export default function PageList() {
	return (
		<Container>
			<Header as="h1">Work in progres...</Header>
		</Container>
	);
}
